# simple-todo-localstorage

Synthetic todo app generated by the /build orchestrator end-to-end test on 2026-04-28.

## How to run

```bash
open synthetic-tests/simple-todo-localstorage/index.html
```

(macOS) Or open the file directly in any modern browser via `file://`. No server, no build step, no dependencies.

## What this is

This is a synthetic test artifact for the gstack `/build` orchestrator chain (`/office-hours` -> `/autoplan` -> `/implement` -> `/qa` -> `/ship`). It is NOT a real product. The seed prompt was deliberately minimal so any failure during the run would be orchestrator-level, not implementation-level. If you opened this expecting a beautiful or feature-rich todo app, it is meant to be tiny.

The app itself works: add / check off / delete todos, persist via `localStorage` under key `simple-todo-localstorage`. Light and dark mode follow your system preference.

## Provenance

- **Run ID:** `91392570-e6b2-4fbb-bbe2-dd7c6b0f9389`
- **Builder slug:** `bilal-ahmed`
- **Company slug:** `simple-todo-localstorage`
- **Run dir:** `~/.gstack/builders/bilal-ahmed/companies/simple-todo-localstorage/runs/91392570-e6b2-4fbb-bbe2-dd7c6b0f9389/`
- **Branch when generated:** `office-hours-implement`
